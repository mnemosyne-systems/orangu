[orangu-coordinator]
host = 127.0.0.1
port = 9000
models = /srv/models
startup_timeout = 180
# max_body_bytes = 67108864
# log_type = console  # console (prints to the terminal) or
                      # file, which appends every line — stamped — to
                      # log_path, and has every profile's orangu-server log
                      # into the same file. What --daemon needs: detached, a
                      # console-logging daemon logs nothing at all
# log_path = orangu-coordinator.log  # the file for log_type = file; defaults
                                     # to orangu-coordinator.log in the
                                     # directory the coordinator was started
                                     # from

[main]
model = ggml-org/gemma-4-E4B-it-GGUF

[explorer]
model = unsloth/Qwen3-Coder-30B-A3B-Instruct-GGUF
